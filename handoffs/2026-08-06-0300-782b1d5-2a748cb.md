## 2026-08-06 03:00 — push 782b1d5..2a748cb (1 commit)

### Design changes
- "Read next" section divider on blog posts now spans full viewport width (edge-to-edge) instead of stopping at `var(--page-max-width)`, matching the page-break pattern used elsewhere for full-bleed dividers.

### Code changes
- `.post-readnext` gains `position: relative` and a `::before` pseudo-element to render the full-width top border, decoupling the divider line from the element's own max-width/padding box.
- Original `border-top` removed from `.post-readnext` and moved onto `.post-readnext::before`, which is sized to `100vw` and centered via `left: 50%; transform: translateX(-50%)`.

### Files touched
- `src/routes/blog/blog.css` (modified) — added full-width divider via `::before` pseudo-element on `.post-readnext`

### Integration notes for Curry
- Prototype-only CSS tweak, no component/prop changes — safe to port as-is into the production blog post template's stylesheet.
- The `100vw` full-bleed trick can cause horizontal scrollbars if the page has a vertical scrollbar cutting into viewport width; verify `overflow-x` on ancestors in production, or swap for a `100vw` + negative-margin-safe technique if issues appear.
- No new tokens introduced — still uses existing `--stroke` and `--page-max-width` / `--gutter` custom properties from `src/index.css`.
