## 2026-08-07 00:23 — push 97497fc..cfa04cf (1 commit)

### Design changes
- Blog post hero (`.post-hero`) now uses the stacked band layout on **all viewports**, not just mobile: a full-bleed mesh gradient band up top carrying the back-link + meta, with the title + description dropped onto the white surface below.
- Band height is now fluid via `height: clamp(300px, 30vw, 440px)` on `.post-hero .bc--hero-bg`, replacing the old fixed desktop overlay and the mobile-only `aspect-ratio: 4 / 3`.
- Removed the full-bleed background + white-fade overlay treatment (`.post-hero-fade`, `linear-gradient` scrim) previously used on desktop to fade the mesh cover into the page background.
- Top-group padding is now fluid: `padding: clamp(96px, 8vw, 132px) var(--gutter) 24px` (was a fixed `196px ... 72px` on desktop).
- Mobile breakpoint no longer redefines the grid/layout — it only tweaks spacing (`.post-hero-top { padding-bottom: 22px }`, `.post-hero-head { padding-top: 28px }`), since the stacked layout is now shared.

### Code changes
- `src/routes/blog/BlogPost.tsx`: removed the `<span className="post-hero-fade" />` element and its rendering; updated the two inline comments describing the hero layout (no longer mobile-vs-desktop conditional).
- `src/routes/blog/blog.css`: moved the stacked-band grid rules (`.post-hero`, `.post-hero-inner`, `.post-hero-top`, `.post-hero-head`) out of the mobile media query and into the base (all-viewport) rule set; deleted the now-unused desktop-only absolute-positioning + fade rules.

### Files touched
- `src/routes/blog/BlogPost.tsx` (modified) — dropped the `.post-hero-fade` span; updated comments to reflect the unconditional stacked-band layout.
- `src/routes/blog/blog.css` (modified) — base `.post-hero` rules rewritten to the grid/band layout (with `clamp()`-based sizing); removed old desktop absolute/fade rules and the duplicate mobile-only grid override.

### Integration notes for Curry
- No new dependencies — pure CSS/JSX restructuring, no package changes.
- The old desktop "overlay + fade-to-white" hero pattern is fully retired; don't port `.post-hero-fade` or the absolute-positioned `.bc--hero-bg` rules forward.
- Both `clamp()` values (`height: clamp(300px, 30vw, 440px)` for the band, `padding: clamp(96px, 8vw, 132px) ...` for the top group) are prototype tuning — sanity-check against DESIGN.md's spacing/breakpoint tokens before locking them in for production, since they were hand-picked for this viewport range rather than derived from a token scale.
- Verify this change against DESIGN.md's hero component spec — if DESIGN.md still documents the old desktop overlay/fade behavior, it needs updating to match this new unconditional stacked-band spec.
