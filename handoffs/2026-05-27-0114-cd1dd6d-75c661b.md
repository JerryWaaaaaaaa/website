## 2026-05-27 01:14 — push cd1dd6d..75c661b (2 commits)

### Design changes
- Hero section height is now fully content-intrinsic — no viewport-relative `min-height`. `padding-top` uses a fluid ratio `calc(100vw * (290/1440) * 0.6)` so the ribbons breathe proportionally to the viewport width.
- `.hero-prompt` card now has `margin-top: 80px` to clear the nav; previously the nav gap was implicit in the old `padding-top: 200px`.
- `.hero-transcript` container repositioned from `top: 80px` to `top: 0` with `padding-top: 80px` so it starts at the section edge and the SVG viewBox can render above its container bounds (via `overflow="visible"`).
- A **second ribbon** is layered behind the prompt card, using a distinct curve path and a separate set of 4 speakers/avatars. It scrolls in the opposite direction (`reverseText`) for visual contrast.
- The mobile override `min-height: 0` and `display: none` on `.hero-transcript` are removed — the ribbons are now shown on mobile.

### Code changes
- `src/sections/HeroTranscript.tsx` — refactored from hard-coded internals to a fully-parameterized component:
  - Exports `Speaker` interface and `DEFAULT_SPEAKERS` / `DEFAULT_CURVE_D` constants.
  - New props: `speakers`, `curveD`, `reverseText`, `textOffsetY`.
  - Uses `useId()` for unique SVG `id` attributes (`pathId`, `clipId`) so multiple instances don't collide.
  - SVG root gets `overflow="visible"` to let the second ribbon's curve extend outside the viewBox.
  - `reverseText` prop inverts the `t` parameter (`1 - t`) so text and avatars scroll the opposite direction.
  - `dominantBaseline="central"` added to `<text>` for proper vertical centering; `dy={textOffsetY}` on `<textPath>` for fine-tuning.
- `src/sections/HeroV2.tsx` — renders two `<HeroTranscript>` instances. Second ribbon uses a separate S-curve (`HERO_TRANSCRIPT_CURVE_2`) that continues from where the first ends (~720,230 → exits top-right), with its own speaker list (`HERO_TRANSCRIPT_SPEAKERS_2`).
- `src/components/RibbonControls.tsx` — `RibbonConfig` and `RIBBON_DEFAULTS` extended with `textOffsetY: number` (default `0`); tuner panel exposes a "Vertical offset" range slider (−10 to 10, step 0.5).
- `src/index.css` — `.hero-v2` padding reworked (see Design changes); mobile `@media` rules simplified.
- Four new avatar PNGs added to `public/avatars/` (Avatar.png, Avatar-1.png, Avatar-2.png, Avatar-3.png) for the second ribbon's speakers.

### Files touched
- `public/avatars/Avatar.png` (new) — speaker avatar "Avery"
- `public/avatars/Avatar-1.png` (new) — speaker avatar "Mateo"
- `public/avatars/Avatar-2.png` (new) — speaker avatar "Mateo"
- `public/avatars/Avatar-3.png` (new) — speaker avatar "Theo"
- `src/components/RibbonControls.tsx` (modified) — added `textOffsetY` to config interface, defaults, snippet, and tuner UI
- `src/index.css` (modified) — hero sizing reworked to intrinsic/content-driven; transcript container layout updated; mobile overrides slimmed
- `src/sections/HeroTranscript.tsx` (modified) — parameterized for multi-instance use (speakers, curve, direction, textOffsetY, unique IDs)
- `src/sections/HeroV2.tsx` (modified) — second ribbon instance with distinct curve, speakers, and `reverseText`

### Integration notes for Curry
- The `RibbonTunerPanel` (floating dev GUI in the bottom-right) and `RibbonControls` are prototype-only debug tools — do not ship them. In production, pass the final tuned values directly as props to `<HeroTranscript>`.
- Avatar images in `public/avatars/` are placeholder PNGs (~7–8 KB each). Replace with production assets or a CDN path; consider AVIF/WebP and responsive `srcSet` in the SVG `<image>` if performance matters.
- The two ribbon curves are hand-tuned SVG path data (constants `DEFAULT_CURVE_D` and `HERO_TRANSCRIPT_CURVE_2`). They're designed for a 1440-wide viewBox — verify they look correct at breakpoints and adjust control points if layout proportions change in production.
- `useId()` ensures safe multi-instance SVG IDs; if you SSR, confirm your React version (18+) hydrates `useId` deterministically to avoid mismatch warnings.
