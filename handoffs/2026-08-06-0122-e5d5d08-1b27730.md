## 2026-08-06 01:22 — push e5d5d08..1b27730 (1 commit)

### Design changes
- Blockquote: removed the `--text-highlight` left border; replaced with an oversized decorative opening quote (`::before`, Georgia serif, 90px desktop / 66px mobile, `rgba(12, 92, 255, 0.2)`) positioned absolutely at the left, plus a small closing quote mark (`::after` on the wrapped `<p>`, 0.78em, `var(--text-highlight)`) placed right after the sentence and before any `<cite>`.
- Callout (`.post-note`): dropped the blue left border, radius bumped from `--rounded-md` to `--rounded-lg`, padding increased slightly (16/20px → 18/22px); inline-code chip inside callouts now uses `var(--bg-neutral)` fill with `var(--bg-accent-dark)` border instead of matching the card fill.
- Code block (`pre`): switched from dark navy (`#0f1836` bg / `#e7ecff` text) to light theme using `var(--bg-accent-light)` bg, `var(--text-primary)` text, and a `1px solid var(--stroke)` border; syntax tokens (`.tok-key/.tok-fn/.tok-str/.tok-com`) recolored for light-panel contrast (purple/blue/green/gray).

### Code changes
- `src/routes/blog/blogData.ts`: all three `<blockquote>` entries now wrap their sentence in a `<p>` tag (required by the new CSS so the closing quote mark, attached via `p::after`, renders before the `<cite>` rather than after it).

### Files touched
- `src/routes/blog/blog.css` (modified) — blockquote, callout, and code-block styling reworked per above.
- `src/routes/blog/blogData.ts` (modified) — wrapped blockquote text content in `<p>` for all 3 posts.

### Integration notes for Curry
- No new dependencies; pure CSS + inline-HTML-string changes, no component logic touched.
- The blockquote closing-quote mark relies on `.post-body blockquote p` being `display: inline` and being the last element before `<cite>` in markup — if you move blog content out of raw HTML strings into JSX/MDX, preserve the `<p>...</p><cite>...</cite>` sibling order or the mark will misplace.
- Callout code-chip color (`var(--bg-neutral)` on `var(--bg-accent-dark)` border) was tuned specifically against `.post-note`'s `var(--bg-accent-light)` fill — re-check contrast if callout background token changes later.
- Blog content is still hardcoded HTML strings in `blogData.ts`; this diff didn't change that architecture, just the string contents — worth flagging if production plans to move to a CMS/MDX pipeline.
