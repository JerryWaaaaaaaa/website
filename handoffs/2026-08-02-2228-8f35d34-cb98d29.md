## 2026-08-02 22:28 — push 8f35d34..cb98d29 (1 commit)

### Code changes
- `.githooks/pre-push`: added a HEAD-didn't-move guard around the handoff-doc generator. Captures `HEAD_BEFORE="$(git rev-parse HEAD)"` before invoking `scripts/generate-handoff.mjs "$RANGE"`, then after a successful (exit 0) run compares `git rev-parse HEAD` against `HEAD_BEFORE`.
- When HEAD is unchanged (generator exited 0 but skipped — range only touches ignored paths, empty diff, etc., so no commit was created), the hook now prints `[handoff] No handoff doc needed for this range; allowing push.` and `continue`s the read loop, letting the push proceed.
- Previously such a skip fell through to the "abort push so the new commit is included" path, which re-triggered the identical range on every retry — an unwinnable loop. This is the bug the change fixes.

### Files touched
- `.githooks/pre-push` (modified) — +12 lines; guards against no-op handoff generation aborting the push into a retry loop.

### Integration notes for Curry
- Local dev-tooling only — this is a git pre-push hook, not shipped UI or app code. Nothing here affects the production microsite build/bundle.
- Depends on `scripts/generate-handoff.mjs` and Node being available at push time; the hook assumes the generator exits 0 on skip (that contract is what this guard compensates for). No new npm packages introduced.
- Hooks only run if the repo's `core.hooksPath` points at `.githooks/` — verify `git config core.hooksPath` is set in the production clone, otherwise this file is inert.
- No design, token, or component changes in this push; `DESIGN.md`, `src/index.css`, `src/sections/`, and `src/components/` are untouched.
