## 2026-06-30 14:55 — push 5cca628..314dea0 (1 commit)

### Design changes
- Backdrop blur on all glass/frosted surfaces is now a hardcoded `blur(16px)` instead of the `--glass-blur` token. The scrolled nav pill, ribbon/slider tuner panels, and the gradient-generator + ribbon-tuner sticky toolbars all render at a fixed 16px blur regardless of the token value.
- The gradient-generator's secondary `rgba(255,255,255,0.5)` chip keeps its `blur(12px)` (unchanged radius).

### Code changes
- Pure CSS edits — no component logic, props, or state changed.
- Replaced `backdrop-filter: blur(var(--glass-blur))` → `backdrop-filter: blur(16px)` across glass surfaces.
- Removed every `-webkit-backdrop-filter` line (both `var(--glass-blur)` and the one `blur(12px)` instance in gradient-generator).

### Files touched
- `src/components/Nav.module.css` (modified) — scrolled pill (`.navWrap.isScrolled .navBar`) blur hardcoded; webkit prefix dropped.
- `src/components/RibbonTunerPanel.css` (modified) — pill + panel surfaces (2 rules) hardcoded to 16px; webkit prefix dropped.
- `src/components/SliderTuner.css` (modified) — pill + panel surfaces (2 rules) hardcoded to 16px; webkit prefix dropped.
- `src/routes/gradient-generator.css` (modified) — sticky toolbar blur hardcoded; webkit prefixes dropped on toolbar + 12px chip.
- `src/routes/ribbon-tuner.css` (modified) — sticky toolbar blur hardcoded; webkit prefix dropped.

### Integration notes for Curry
- No new dependencies; no install step. CSS-only.
- **Token regression to decide on:** `--glass-blur` is no longer referenced by these surfaces. In production, prefer restoring the token (`blur(var(--glass-blur))`) so blur stays centrally controllable — confirm with Jerry whether 16px is the intended canonical value to fold back into the token, or a one-off.
- **Webkit prefix removed:** `-webkit-backdrop-filter` was dropped. Modern Safari supports unprefixed `backdrop-filter`, but verify the target browser matrix — if older Safari/iOS support is required in production, re-add the prefix.
- Glass surfaces still rely on `--glass-bg`, `--stroke`/`--border-subtle` tokens (unchanged); only the blur radius source changed.
