## 2026-07-20 23:47 — push 44f3ac5..fb318ff (1 commit)

### Design changes
- **Hub product now uses brand blue.** `public/Icon/Hub.svg` recolored from near-black `#000001` → `--color-blue` `#0C5CFF` (DESIGN.md brand blue) on all 4 paths.
- **Hub nav card retinted** to match the other product cards' convention: card + icon backgrounds changed from opaque greys (`#f1f4f6` / `#dfe3e8`) to `rgba(12, 92, 255, 0.06)` — the same 6%-tint pattern used by Sheets/Datatable/Classicdocs cards.
- **Paper mockup panel label** renamed "AI Companion" → "Ask AI" in the Paper product mockup.
- **Hero default variant** switched from `b` (Grouped, `<HeroV5B/>`) to `a` (Flat, `<HeroV5/>`).
- **Paper hero asset refreshed** (`paper-ui.png`, re-exported ~550KB → 312KB) and inline-AI screenshot updated; an unused deliverable image was removed.

### Code changes
- `src/sectionVariants.tsx` — Hero variant group `defaultId: 'b'` → `'a'`; renders `<HeroV5/>` by default. Other groups unchanged.
- `src/sections/productSuite/PaperMockup.tsx` — `.ppm-panel-title` text string "AI Companion" → "Ask AI" (line ~318). No logic/prop changes.
- `src/components/Nav.module.css` — `.productCardHub` and `.productCardHub .productIcon` backgrounds updated to `rgba(12, 92, 255, 0.06)`.

### Files touched
- `public/Icon/Hub.svg` (modified) — fill recolored to `#0C5CFF` on all paths
- `src/components/Nav.module.css` (modified) — Hub card/icon backgrounds → 6% blue tint
- `src/sectionVariants.tsx` (modified) — Hero default variant `b` → `a`
- `src/sections/productSuite/PaperMockup.tsx` (modified) — panel title "AI Companion" → "Ask AI"
- `public/hero-browser/paper-ui.png` (modified) — re-exported paper hero asset (smaller)
- `public/core-features/inline-ai-01.png` (modified) — updated screenshot
- `public/core-features/meeting-deliverable.png` (deleted) — unused asset removed

### Integration notes for Curry
- No new dependencies — pure asset + token/string edits. Nothing to install.
- The hex/rgba values are hardcoded here but map to `--color-blue` (`#0C5CFF`) in DESIGN.md/`src/index.css`. In production, reference the token/CSS var rather than inlining `rgba(12, 92, 255, 0.06)` and `#0C5CFF`.
- The Hero `defaultId` flip is prototype variant-tuner config (`sectionVariants.tsx` drives the in-prototype variant switcher, which won't exist in prod). Treat it as "ship the Flat hero (`HeroV5`)"; the other hero variants (`HeroV5B/V2/V4`) are prototype-only explorations.
- Confirm the deleted `meeting-deliverable.png` isn't referenced anywhere before dropping it from prod assets — it was removed as unused, verify against your final section set.
