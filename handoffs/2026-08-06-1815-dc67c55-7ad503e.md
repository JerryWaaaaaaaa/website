Now I'll write the handoff.

## 2026-08-06 18:15 — push dc67c55..7ad503e (26 commits)

### Design changes
- New blog homepage: white page (`--bg-neutral`) with a subtle blue dot-grid texture, a masthead capped at 560px so the h1 wraps to two lines, a row of product-filter pills, a featured post rendered as a light-blue (`#eef4ff`, between `--bg-accent-light`/`--bg-accent-medium`) card with text left / square 1:1 mesh-gradient cover right (flush, only right corners rounded), and a 3-column grid of square gradient-tile cards (title always visible, description + author revealing on hover/focus, shown by default on touch).
- Blog detail page: full-bleed mesh-gradient hero (`.bc--hero-bg`, non-square, 1120×520) behind meta/title/description, with a linear fade into `--bg-neutral` as the reader scrolls (mirrors the homepage hero-to-white handoff); author added as byline lead in the meta row; description demoted from "lede" styling to match body copy (18px/1.7).
- Full `.post-body` typographic system added for rendered markdown: h2–h4, bold/italic, `mark` highlight (`--bg-highlight-pink`), inline links, inline code + light-scheme syntax-highlighted code blocks, ordered/unordered/nested lists with tinted markers, blockquote with an oversized decorative opening quote (centered on the paragraph, not the citation) and a small closing quote in the text color, callout/note card, data table with caption, horizontal rule. Mobile overrides included.
- Covers are generated via the Gradient Generator's `MeshCanvas` + `DEFAULT_PALETTE` (deterministic per slug via string hash), layered with a grain/dither SVG texture; title text is deep navy (`#0f1836`) for legibility on the pale palette.
- Navbar redesign: top-level tabs are now **Products, Explore, Templates, Blog** (Features tab removed). Explore is a floating anchored dropdown card (Use cases / Blog / Help center), not a full-width expanding panel like Products. Mirrored in the mobile drawer as an accordion.
- Product filter pills on the blog index reuse each product's brand color (`--pc` custom property) to tint the active pill, matching the product-suite tab bar's color treatment.

### Code changes
- `src/data/products.ts` (new): single source of truth for product identity (`key`, `label`, `icon`, `color`) — shared by `ProductSuiteV5D` and the blog filter so icon/name/color stay in sync.
- `src/sections/ProductSuiteV5D.tsx`: refactored to build its product list from `PRODUCTS` (identity) plus a local screenshot map; no visual change intended.
- `src/routes/blog/blogData.ts` (new): mock post data (`POSTS`, `BlogPost` type) with `sortedPosts`, `authorFor`/`initials` (deterministic mock authors with role), `formatDate`/`shortDate`, and an optional `product: ProductKey` field for filtering. Two posts ("500M documents" and "real-time collaboration") were rewritten as kitchen-sink showcases of every supported body element. `body` is trusted HTML rendered via `dangerouslySetInnerHTML` — no sanitization, prototype-only.
- `src/routes/blog/cover.ts` (new): `hash(seed)` + `meshFor(seed)` deterministically pick a `MeshPreset` from `DEFAULT_MESH_PRESETS`; exports `GRAIN_DATA_URI` (inline SVG turbulence filter, grayscale).
- `src/routes/blog/BlogCover.tsx` (new): wraps `MeshCanvas` (from the gradient-generator route) + grain overlay + optional centered `text` label or arbitrary `children` overlay; accepts `size` (square) or explicit `width`/`height` (e.g. the hero).
- `src/routes/blog/PostCard.tsx` (new): exports `PostMeta` (meta row, `showReadTime`/`showAuthor` opt-ins), `CardText` (shared eyebrow/title/description/author block used by both featured panel and cards), `PostCard` (grid card = gradient tile with hover-reveal content).
- `src/routes/blog/BlogIndex.tsx` (new): reads `?product=` and `?page=` from `useSearchParams`; `productFor()` validates the param and falls back to "All" on anything invalid; featured post only shown on page 1 of the unfiltered view; pagination at `PER_PAGE = 9`.
- `src/routes/blog/BlogPost.tsx` (new): resolves post by `:slug`, builds a 3-post "Read next" row (wrapping index), sets/resets `document.title`.
- `src/components/Nav.tsx`: added `Panel = 'products' | 'explore' | null` state, `EXPLORE_ITEMS` config, `exploreMenu` dropdown markup + matching mobile-drawer accordion section; added a direct Blog link (desktop + mobile); scrolled-pill `overflow: hidden` clip now scoped to `productsOpen` only so the floating Explore menu isn't clipped.
- `src/App.tsx`: registered `/blog` → `BlogIndex` and `/blog/:slug` → `BlogPost`.
- Design/agent tooling: installed then pruned Claude Code skills (`.agents/skills/fixing-accessibility`, `fixing-metadata`, `userinterface-wiki`, `.claude/commands/rams.md`); added a `SessionStart` hook (`.claude/hooks/session-start.sh` running `scripts/install-hooks.sh`) so `.githooks` (pre-push handoff generator) activates on fresh cloud clones — no production relevance, tooling-only.

### Files touched
- `src/routes/blog/BlogIndex.tsx` (new) — blog homepage: masthead, product filter, featured card, grid, pagination.
- `src/routes/blog/BlogPost.tsx` (new) — blog detail page: gradient hero, rendered body, read-next row.
- `src/routes/blog/PostCard.tsx` (new) — `PostMeta`, `CardText`, `PostCard` shared components.
- `src/routes/blog/BlogCover.tsx` (new) — generated mesh-gradient cover component.
- `src/routes/blog/cover.ts` (new) — deterministic mesh-preset picker + grain texture data URI.
- `src/routes/blog/blogData.ts` (new) — mock post data, author helpers, date helpers.
- `src/routes/blog/blog.css` (new, 781 lines) — full blog page/detail/typography stylesheet.
- `src/data/products.ts` (new) — shared product identity (key/label/icon/color).
- `src/sections/ProductSuiteV5D.tsx` (modified) — sources product list from `products.ts`.
- `src/components/Nav.tsx` (modified) — Explore dropdown, Blog tab, Features tab removed, mobile drawer parity.
- `src/components/Nav.module.css` (modified) — floating dropdown styles, scoped overflow clip.
- `src/App.tsx` (modified) — `/blog` and `/blog/:slug` routes.
- `.agents/skills/**`, `.claude/**`, `skills-lock.json` (new/modified) — agent tooling only, no runtime/UI impact.

### Integration notes for Curry
- **No CMS**: all blog content is hardcoded in `src/routes/blog/blogData.ts` with `body` as raw trusted HTML strings rendered via `dangerouslySetInnerHTML`. Before production, this needs a real content source and either a markdown pipeline or sanitization (DOMPurify etc.) if HTML ever becomes user- or CMS-editable.
- Blog covers depend on `src/routes/gradient-generator/MeshCanvas` and `presets`/`mesh` modules (`DEFAULT_PALETTE`, `DEFAULT_MESH_PRESETS`) — these are prototype/demo-route code, not obviously meant for production reuse; confirm with Jerry whether the gradient generator route ships or gets extracted into a shared lib.
- `src/data/products.ts` is now the single source of truth for product key/label/icon/color, consumed by both `ProductSuiteV5D` and the blog filter — keep using it rather than re-hardcoding product lists if adding more product-scoped surfaces.
- The Explore dropdown's "Use cases" and "Help center" links are still `href="#"` placeholders; only "Blog" is wired to a real route. Confirm final destinations with Jerry before launch.
- `blog.css` is a large (781-line) standalone stylesheet built entirely from existing `src/index.css` custom properties — no new tokens were added to DESIGN.md/`index.css`, so no token file changes are needed, but double-check `--bg-accent-*` naming/usage stays consistent if DESIGN.md tokens are renamed later.
