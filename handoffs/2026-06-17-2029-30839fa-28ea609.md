## 2026-06-17 20:29 — push 30839fa..28ea609 (1 commit)

### Design changes
- New "V5" page variant added to the version switcher — a full-page composition of five sections: Hero, ProductSuite, BuiltDifferentiate, OpenPlatform, UseCase.
- Hero uses `--gradient-accent` background, `--text-secondary` for subtitle/suite labels, and `--color-blue` product-name tints per product (Sheets=#019F5C, Canvas/Paper=#3579FD, Slides=#FB327E).
- ProductSuite arc carousel uses `--bg-accent-light` surface, `--bg-accent-medium` highlight pill, `--stroke` border on the screenshot frame.
- BuiltDifferentiate scroll-driven sticky layout uses `--bg-accent-light` surface; step placeholder tints reference `--bg-accent-medium`, `--bg-highlight-blue`, `--bg-highlight-pink`, `--gradient-accent`, `--bg-accent-dark`, `--color-blue`.
- OpenPlatform cards use `--bg-accent-light` card fill, `--text-highlight` overline color.
- UseCase folder tabs use `--bg-accent-light` active tab/panel fill, `--bg-accent-medium` accent band and chip, `--bg-highlight-pink` prompt pill, `--bg-neutral` card backgrounds, `--text-highlight` for headline lead + active tab color.
- Typography across all V5 sections: headings at 44px/500wt/−0.02em tracking (General Sans), body at 18px/400wt/1.4 line-height — consistent with existing DESIGN.md type scale.

### Code changes
- **`src/pageVersions.tsx`** — added `'v5'` to the `VersionId` union type; added a new entry in `PAGE_VERSIONS` that renders `<HeroV5 /> → <ProductSuiteV5 /> → <BuiltDifferentiateV5 /> → <OpenPlatformV5 /> → <UseCaseV5 />` in sequence. Imports five new V5 section components.
- **`src/sections/HeroV5.tsx`** — ported from V3 hero pattern. Rotates through 4 products (Sheets, Canvas, Slides, Paper) on a 2200ms interval via `useState`/`setInterval`. Renders a browser-window chrome with traffic lights, animated URL pill, cross-fading product screenshots, and a floating meeting video (`<video autoPlay muted loop>`). Below the hero row, renders a 9-item product suite badge row (icons from `public/Icon/`). Respects `prefers-reduced-motion`. Uses `Button` from `src/components/Button`.
- **`src/sections/ProductSuiteV5.tsx`** — fanned-arc carousel of 5 products (Paper, Canvas, Slides, Sheets, Data table). Uses `useLayoutEffect` + Web Animations API to animate icon positions along a polar arc (R=450px, 15° step). Active product is centred with a highlight pill; wrapping items exit one edge and re-enter the other with an opacity fade. Default selection is Slides. Product screenshot cross-fades below. ARIA `role="tablist"` + `role="tab"`.
- **`src/sections/BuiltDifferentiateV5.tsx`** — scroll-driven sticky layout with 6 key-point steps in a left column. Uses `IntersectionObserver` (rootMargin `−50% 0px`) to detect which step crosses the viewport centre and sets `activeIndex`. Right column is `position: sticky; height: 100vh` with crossfading colored placeholder divs. Mobile: stacks vertically, inline media per step, no sticky.
- **`src/sections/OpenPlatformV5.tsx`** — static 3-card grid (Open API, MCP, Zapier). Cards have overline/title/body/CTA or logo row. Uses `btn btn-primary` class for CTAs (not `Button` component). Logos for MCP card loaded from `/integration-logos/`.
- **`src/sections/UseCaseV5.tsx`** — folder-style tabs for 6 personas (Sales, Recruiters, Consultants, Coaches, Agencies, Product managers). Active tab shows a two-tone headline + persona image panel. Below: a pink prompt pill ("Find your role's perfect AI template") + a horizontally-masked strip of 6 template cards with file-type icons (doc/slide/table) rendered via inline `FileTypeIcon` component. Uses `Button` from `src/components/Button`.
- **`public/Icon/Video.svg`** — new blue video camera icon (48×48) used as the "Meeting" badge in the hero suite row.

### Files touched
- `public/Icon/Video.svg` (new) — blue video/camera SVG icon for the Meeting suite badge
- `src/pageVersions.tsx` (modified) — added V5 version entry and imports
- `src/sections/HeroV5.tsx` (new) — hero section with rotating products + suite badge row
- `src/sections/HeroV5.css` (new) — hero styles, browser chrome, responsive breakpoints at 1199px/639px
- `src/sections/ProductSuiteV5.tsx` (new) — arc carousel with Web Animations API transitions
- `src/sections/ProductSuiteV5.css` (new) — arc stage, glow/pill decorations, responsive at 900px/560px
- `src/sections/BuiltDifferentiateV5.tsx` (new) — scroll-driven sticky section with IntersectionObserver
- `src/sections/BuiltDifferentiateV5.css` (new) — sticky layout, responsive column collapse at 900px/560px
- `src/sections/OpenPlatformV5.tsx` (new) — static 3-card integration grid
- `src/sections/OpenPlatformV5.css` (new) — card grid, responsive at 1199px/899px/639px
- `src/sections/UseCaseV5.tsx` (new) — tabbed persona panels + template card strip
- `src/sections/UseCaseV5.css` (new) — folder tabs, accent band pseudo-element, responsive at 1199px/899px/639px
- `tsconfig.app.tsbuildinfo` (modified) — build cache updated

### Integration notes for Curry
- **All media placeholders are colored divs or static images, not final assets.** BuiltDifferentiate steps use CSS variable tints as placeholder backgrounds — swap these for real screenshots/videos. ProductSuite and Hero reference PNGs at `/hero-browser/*.png` and a video at `/hero-may-images/meeting-floating.mp4`; verify these exist in production or replace.
- **OpenPlatformV5 uses raw `btn btn-primary` CSS classes** for its CTAs instead of the shared `<Button>` component used everywhere else. Normalize to whichever pattern production adopts.
- **ProductSuiteV5 relies on the Web Animations API** (`Element.animate()`) for the arc carousel spin — no polyfill is included. All target browsers should support it, but verify against your browser matrix.
- **No new npm dependencies.** All V5 sections import only React and the existing `Button` component. Font dependency is `General Sans` (already in the project). Icon paths assume the existing `public/Icon/` and `public/integration-logos/` directories are deployed.
