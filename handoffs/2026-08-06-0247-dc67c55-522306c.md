## 2026-08-06 02:47 — push dc67c55..522306c (13 commits)

### Design changes
- New `/blog` and `/blog/:slug` routes, styled via new `src/routes/blog/blog.css` on top of existing tokens (`--bg-accent-light/medium/dark`, `--text-primary/secondary/highlight`, `--rounded-lg/3xl`, `--page-max-width`, `--gutter`, `--section-block`, `--stroke`, `--ease-out`). No new tokens were added to `DESIGN.md` or `src/index.css` — the blog reuses existing shade/accent scale.
- Page background is white (`--bg-neutral`) with a subtle dot-grid texture (`radial-gradient` dots, 20px grid) on `.blog-root`; masthead reads "Updates on AI Productivity Suite".
- Featured post is a card: light-blue panel (`--bg-accent-medium`) on the left with eyebrow (date + category) + title anchored top, description + author anchored bottom; a square 1:1 generated cover is flush against the right edge with only its right corners rounded (`.bc--featured`).
- Grid cards (`.blog-card`) are the gradient cover itself (1:1, `.bc--card`) — title shown at rest; description + author avatar/name/role reveal on hover/focus via `.bc-card-reveal` (always visible on touch, no hover). Hovering also scales the canvas gradient 1.04x (not the text overlay).
- Covers are generated, deterministic-per-slug mesh gradients (`MeshCanvas` + `DEFAULT_MESH_PRESETS`, reused from the Gradient Generator) using the generator's `DEFAULT_PALETTE`, layered with an SVG grain/dither texture (`GRAIN_DATA_URI`) and, where no children are passed, a centered deep-navy (`#0f1836`) title label on a soft white scrim.
- Detail page (`BlogPost`) adds top padding so the title clears the fixed nav, shows a square hero cover, renders mock HTML body content, and ends with a "Read next" section (3 related posts) that now has bottom padding added (`.post-readnext` `padding: 48px var(--gutter) 88px`, the final commit in this push).
- Responsive: grid drops to 2 cols at ≤900px (featured stacks, cover on top at 16:10) and 1 col at ≤560px with reduced top padding.

### Code changes
- `src/routes/blog/blogData.ts` — mock data module (no CMS): `POSTS` array (11 posts, `slug/title/category/date/readTime/excerpt/coverText?/author?/body`), plus `sortedPosts()`, `formatDate()`, `shortDate()`, deterministic `authorFor()`/`initials()` helpers (author/role assigned by hashing the slug when `post.author` is unset).
- `src/routes/blog/cover.ts` — `meshFor(seed)` hashes a string to deterministically pick a `MeshPreset` from the Gradient Generator's `DEFAULT_MESH_PRESETS`; exports `GRAIN_DATA_URI` (inline SVG fractal-noise texture).
- `src/routes/blog/BlogCover.tsx` — `<BlogCover slug text? children? className size>`: renders `<MeshCanvas>` at a given resolution + grain overlay; renders `children` if provided, else a centered `text` label with scrim. Aspect ratio driven by `className` (`bc--card` / `bc--featured` / `bc--hero`).
- `src/routes/blog/PostCard.tsx` — exports `PostMeta` (category/date/readtime row), `CardText` (shared eyebrow/title/description/author block used by the featured card), and `PostCard` (grid card wrapping `BlogCover` with hover-reveal content).
- `src/routes/blog/BlogIndex.tsx` — homepage: featured post (page 1 only) + paginated grid (`PER_PAGE = 9`) via `?page=` search param, scroll-to-top on page change.
- `src/routes/blog/BlogPost.tsx` — detail page: resolves post by slug, sets `document.title` (restored on unmount), computes "Read next" as the next 3 posts wrapping around, renders body via `dangerouslySetInnerHTML` (mock/trusted content only).
- `src/App.tsx` — registers `/blog` → `BlogIndex` and `/blog/:slug` → `BlogPost`.
- Unrelated to the blog: added `.claude/hooks/session-start.sh` (invoked via new `SessionStart` hook in `.claude/settings.json`) which runs `scripts/install-hooks.sh` on every session start so cloud/fresh-clone sessions re-point `git config core.hooksPath` at `.githooks/` (fixes the pre-push handoff-doc generator not running in fresh containers).
- Installed/pruned Claude Code design-review skills: net-added `fixing-accessibility`, `fixing-metadata`, `userinterface-wiki` (kept), while `improve-ui`, `ui-skills-root`, `fixing-motion-performance`, `baseline-ui` were installed then removed same push; `skills-lock.json` reflects the final kept set (plus pre-existing `emil-design-eng`, `review-animations`, `rams`, and others unrelated to this push).

### Files touched
- `src/routes/blog/BlogCover.tsx` (new) — generated cover component (mesh gradient + grain + optional label/children)
- `src/routes/blog/BlogIndex.tsx` (new) — blog homepage (featured + paginated grid)
- `src/routes/blog/BlogPost.tsx` (new) — blog detail page + "Read next"
- `src/routes/blog/PostCard.tsx` (new) — `PostMeta`, `CardText`, `PostCard`
- `src/routes/blog/blogData.ts` (new) — mock posts + date/author helpers
- `src/routes/blog/cover.ts` (new) — deterministic mesh-preset picker + grain data URI
- `src/routes/blog/blog.css` (new) — all blog styling (~430 lines)
- `src/App.tsx` (modified) — added `/blog` and `/blog/:slug` routes
- `.claude/hooks/session-start.sh`, `.claude/settings.json` (new) — reactivates `.githooks` every session start
- `.agents/skills/**`, `.claude/skills/**`, `skills-lock.json` (new/modified) — design/a11y/SEO review skills for Claude Code agents (not app runtime code)
- `handoffs/2026-07-20-2029-eb47b72-94f247f.md` (new) — auto-generated handoff doc from the git hook itself, informational only

### Integration notes for Curry
- Blog data is 100% mock (`blogData.ts`), including `dangerouslySetInnerHTML` body content that is hardcoded/trusted — production needs a real CMS/data source and either sanitization or a safe renderer if body content ever becomes user- or CMS-authored.
- Covers are computed client-side at render time (canvas mesh gradient + hashed preset/author selection) — no images are generated or stored; if production wants static/cacheable cover images, this'll need to move to build-time or server-side generation.
- No new design tokens were introduced; the blog exclusively reuses `src/index.css` custom properties, so there's nothing to reconcile against `DESIGN.md`.
- The `.claude/`, `.agents/`, `skills-lock.json`, and hook changes are Claude-Code-agent tooling only (skills for future AI-assisted dev sessions) — not part of the shipped site and can be ignored/dropped in a production rebuild.
