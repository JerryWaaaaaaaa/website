## 2026-08-06 17:56 — push 2c6c96b..d425574 (1 commit)

### Design changes
- Blog masthead now shares the same 560px max-width as its description text (previously only the description was capped), so the `<h1>` wraps to two lines instead of running full-bleed edge-to-edge above the subtitle.

### Code changes
- Moved the `max-width: 560px` constraint from `.blog-masthead__description` up to the parent `.blog-masthead` container in `src/routes/blog/blog.css`, so both the title and description inherit one shared column width.

### Files touched
- `src/routes/blog/blog.css` (modified) — moved `max-width: 560px` from the description rule to `.blog-masthead`

### Integration notes for Curry
- Pure CSS change, no component/prop/state changes — nothing new to wire up.
- Verify 560px still matches the production breakpoint/grid for the blog route; this value was carried over as-is from the prototype's description rule, not re-derived from DESIGN.md.
- Check responsive behavior below 560px viewport width — no media query changes were made here, so confirm the masthead doesn't need additional constraints on small screens.
