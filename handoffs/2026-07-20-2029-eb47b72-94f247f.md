## 2026-07-20 20:29 — push eb47b72..94f247f (3 commits)

### Code changes
- No application code was touched — `src/sections/`, `src/components/`, `design-system/`, and `DESIGN.md` are all unchanged in this push.
- Installed a set of Claude Code agent skills for design/a11y/motion review (`fixing-accessibility`, `fixing-metadata`, `userinterface-wiki`), pruning an initially-broader set (`improve-ui`, `ui-skills-root`, `fixing-motion-performance`, `baseline-ui`) once found to overlap with skills already present (`emil-design-eng`, `review-animations`) or with `rams`.
- Added `.claude/commands/rams.md`, a `/rams` slash command for design-quality review.
- Added `.claude/hooks/session-start.sh`, wired via `.claude/settings.json` as a `SessionStart` hook, which runs `scripts/install-hooks.sh` on every session start to set `core.hooksPath=.githooks`. This fixes cloud/container sessions being fresh clones where that git config doesn't persist, which previously meant the `.githooks/pre-push` handoff-note generator silently never ran.
- `skills-lock.json` pins the final installed skill set/versions.

### Files touched
- `.claude/hooks/session-start.sh` (new) — SessionStart hook script, re-runs `scripts/install-hooks.sh` to re-point git at `.githooks`
- `.claude/settings.json` (new) — registers the SessionStart hook
- `.claude/commands/rams.md` (new) — `/rams` design-quality review command
- `.claude/skills/fixing-accessibility`, `fixing-metadata`, `userinterface-wiki` (new, symlinks) — activates the corresponding `.agents/skills/*` packages
- `.agents/skills/fixing-accessibility/SKILL.md`, `.agents/skills/fixing-metadata/SKILL.md` (new) — a11y/SEO fix-it reference guides
- `.agents/skills/userinterface-wiki/AGENTS.md`, `SKILL.md`, `rules/*.md` (new, ~150 files) — UI/UX/animation/audio/typography best-practice rule library, reference-only
- `skills-lock.json` (new) — pins the installed skill set

### Integration notes for Curry
- Nothing in this push affects the UI you're rebuilding — safe to skip entirely for the production port.
- It's purely Claude Code tooling: agent-facing design/a11y review skills plus a hook fix so the repo's pre-push handoff-note generator actually runs in fresh cloud sessions.
- No new npm dependencies, no build or runtime changes.
- If you want the same skills/session hook in your own environment, the relevant files are `scripts/install-hooks.sh` and `.claude/settings.json`, but that's orthogonal to the rebuild work.
