## 2026-07-01 23:57 — push ec05b82..6360905 (1 commit)

### Design changes
- Adjusted the inline dark screenshare control chip's vertical alignment relative to the surrounding title text: `vertical-align` changed from `-0.16em` to `-0.05em`, raising the chip ~0.11em so it sits more centered on the text baseline instead of dropping below it.
- Purely a baseline-alignment nudge on the `.kp-chip` inline element — no color, size, or type-token changes. Chip remains `1.16em × 1em`, `#131619` background, `0.14em` radius.

### Code changes
- No component logic, prop, or state changes. Single CSS property value edit in `src/sections/builtDifferentiate/TitleDecorations.css` (`.kp-chip` rule).

### Files touched
- `src/sections/builtDifferentiate/TitleDecorations.css` (modified) — `.kp-chip` `vertical-align` `-0.16em` → `-0.05em`.

### Integration notes for Curry
- No dependencies to install; this is a one-line CSS tweak with zero runtime/JS impact.
- The chip is sized in `em` units so it scales with the title font-size — the `vertical-align` value is tuned against the `builtDifferentiate` title type scale specifically; re-verify alignment if you change that title's font-size or line-height in production.
- `.kp-chip` is the "screenshare control" decoration that renders inline within the section title alongside `.kp-hand` (pointing-hand cursor, absolutely positioned at the chip's lower-right). Keep them together — the hand is positioned relative to the chip.
- Prototype-only: these `TitleDecorations` are hardcoded inline SVG/icon flourishes for the `builtDifferentiate` heading, not a generalized component — expect to formalize them into a reusable primitive when rebuilding.
