## 2026-06-16 22:46 — push f4be3fa..0325940 (1 commit)

### Design changes
- Hero product tabs now auto-rotate every 1.5 s, cycling through all 8 surfaces (Slides → Sheets → Data table → Canvas → Paper → Hub → Mail → Calendar) so the visitor sees the full product suite without interaction.

### Code changes
- `src/sections/HeroV4.tsx` — added a `useEffect` with a `setInterval` (1 500 ms) that advances the `active` tab state to the next entry in the `TABS` array, wrapping around via modulo. The interval is cleaned up on unmount.
- Added `useEffect` to the React import list.

### Files touched
- `src/sections/HeroV4.tsx` (modified) — auto-advance timer added to `HeroV4` component

### Integration notes for Curry
- The 1 500 ms interval is hardcoded; in production consider extracting it to a constant or prop and tuning with motion/UX (current pace is fast — good for demos, possibly too fast for real visitors).
- The timer does **not** pause on hover or when the user manually clicks a tab — production should clear/reset the interval on user interaction to avoid fighting the auto-rotation.
- No new dependencies; this is pure React state + `setInterval`.
- Prototype-only concern: the `TABS` array includes placeholder screenshots for Hub, Mail, and Calendar (`canvas-ui.png` reused) — production will need real assets before this rotation looks complete.
