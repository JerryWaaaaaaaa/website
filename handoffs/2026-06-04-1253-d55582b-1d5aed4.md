## 2026-06-04 12:53 — push d55582b..1d5aed4 (1 commit)

### Design changes
- Hero product showcase now renders inside a persistent **browser-window chrome** (matches Figma node `3208:9370`) with 24px border-radius, 1px border `rgba(2,6,31,0.08)`, and box-shadow `0 24px 64px rgba(12,92,255,0.14)`.
- Nav bar: macOS traffic-light dots (close `#ed6a5e`, minimize `#f4bd50`, zoom `#61c454`), centered URL pill (`#f3f5f9` bg, 999px radius, 14px/500 text `#4c4c4c`).
- Paper texture overlay at 89% opacity applied via `::before` pseudo on the browser frame.
- Content area uses `aspect-ratio: 2352/1514`, 20px inner border-radius, 1px border `#e3edfc`.
- **Skeleton-load** system: per-product shimmer placeholders (gradient `#e7ebf2 → #f6f8fb → #e7ebf2`, 1.1s linear infinite) that dissolve over 700ms (`sk-reveal` keyframe, holds at full opacity 0–60%, fades 60–100%).
- URL text fades in with a 600ms ease animation on each rotation.
- Tablet breakpoint (`≤1199px`): `.hero-v3-inner` now uses `align-items: center` instead of `stretch`.

### Code changes
- **`src/sections/HeroV3.tsx`** — `PRODUCTS` array gains `url` (string) and `layout` (union `'slides'|'sheets'|'canvas'|'datatable'`) fields per product. Product images updated to new `/hero-browser/*.png` paths.
- New `SkeletonShapes` component renders layout-specific placeholder silhouettes (thumbnail rail for slides, 6×8 cell grid for sheets, document lines for canvas, 4-column kanban for datatable).
- Browser chrome JSX added: nav bar with traffic dots + centered URL pill (keyed on `activeIndex` to replay fade). Content area wraps existing crossfade `<img>` stack plus a skeleton overlay div keyed on `activeIndex` (remount triggers one-shot CSS animation).
- **`src/index.css`** — `.hero-v3-product` class renamed to `.hero-v3-browser`; ~270 new lines for browser chrome, skeleton layout variants, shimmer/reveal keyframes.
- Reduced-motion media query hides skeleton entirely (`display:none`).

### Files touched
- `public/hero-browser/canvas-ui.png` (new) — product screenshot for Canvas
- `public/hero-browser/datatable-ui.png` (new) — product screenshot for Data table
- `public/hero-browser/left-icon-set.svg` (new) — nav/back-forward arrows SVG (not currently rendered in JSX)
- `public/hero-browser/refresh.svg` (new) — URL bar refresh icon
- `public/hero-browser/sheets-ui.png` (new) — product screenshot for Sheets
- `public/hero-browser/slides-UI.png` (new) — product screenshot for Slides
- `public/hero-browser/texture-overlay.png` (new) — paper grain texture for browser frame
- `src/index.css` (modified) — browser chrome styles, skeleton system, keyframes, responsive fixes
- `src/sections/HeroV3.tsx` (modified) — browser chrome markup, skeleton component, updated product data

### Integration notes for Curry
- `left-icon-set.svg` is committed but **not referenced** in the JSX — Jerry may have planned to add it to the nav bar; confirm whether it belongs in production or can be dropped.
- The skeleton shimmer relies on CSS `animation` replay via React key remount (`key={activeIndex}`); if you move to a server component or framework that doesn't remount, you'll need an alternative trigger (e.g., toggling a class).
- Product screenshots are large unoptimized PNGs (up to 1.2 MB). For production, convert to WebP/AVIF and serve responsive `srcSet` variants or use an image CDN.
- `ROTATION_INTERVAL` is hard-coded at 2200ms in the component — coordinate with design on whether the skeleton reveal timing (700ms) should be a shared token or remain independent.
