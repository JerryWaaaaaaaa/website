Now I have enough detail to write the handoff note.

## 2026-08-06 04:43 — push dc67c55..a6acefb (21 commits)

### Design changes
- New blog vertical: white page background with a subtle dot-grid texture (`.blog-root`), generous top padding on both listing and detail pages so titles clear the fixed nav.
- Featured post renders as a card: `--bg-accent-medium` panel, text left / square (1:1) gradient cover right with only the right corners rounded (`var(--rounded-3xl)`); no border, contrast comes purely from the tint.
- Every card (featured + grid) shares one top/bottom layout: eyebrow (short date + category) + title anchored top, description + author (avatar initials, name, role) anchored bottom, generous internal spacing.
- Grid cards are the gradient tile itself (no separate title/meta block underneath): title shows at rest, description + author reveal on hover/focus, and are shown by default on touch devices (no hover).
- Covers are auto-generated, deterministic per slug: a small string hash picks one of the gradient generator's `MESH` presets (`meshFor` in `cover.ts`), rendered via `<MeshCanvas>` with the generator's `DEFAULT_PALETTE`, then layered with an SVG grain/dither texture (`GRAIN_DATA_URI`). Same palette as the homepage hero's `--gradient-accent`/Gradient Generator output.
- Blog detail header: the post's cover becomes a full-bleed, non-square gradient hero background behind meta/title/description, with a linear fade to white on scroll (mirrors the v5 hero-to-white handoff pattern elsewhere on the site). Description now matches body-copy style (18px/1.7) rather than a larger lede.
- Full markdown/typography system added for `.post-body`: h2–h4, bold/italic, `<mark>` highlight (lavender), inline links, inline code + dark-token-colored code blocks (later flipped to a light accent-fill scheme with light-theme token colors), ordered/unordered/nested lists with custom markers, blockquote with an oversized decorative opening quote mark centered on the paragraph + small closing mark (colored to match quote text, not brand blue) and citation support, inline figure/caption, callout note card (no colored left border, white inline-code chip), data table with caption, horizontal rule, plus mobile-specific tweaks for headings/code/tables.
- "Read next" section on detail pages: full-width divider, extra bottom padding.
- Navbar restructured: top-level tabs are now **Products, Explore, Templates, Blog** (Features tab removed). Explore is a floating anchored dropdown card (Use cases, Blog, Help center) rather than a full-width expanding panel — visually distinct from Products, which keeps the expanding product-grid panel. Mobile drawer mirrors this: Explore becomes an accordion, Templates/Blog become direct rows.

### Code changes
- New route module `src/routes/blog/` wired into `src/App.tsx` as `/blog` (`BlogIndex`) and `/blog/:slug` (`BlogPost`).
- `BlogIndex.tsx`: reads `sortedPosts()` from mock data, splits featured (post[0]) from a paginated grid (`PER_PAGE = 9`), page state driven by `?page=` search param, scrolls to top on page change.
- `BlogPost.tsx`: resolves post by slug param, computes a 3-post "Read next" row (wraps around the sorted list, filters out the current post), sets `document.title` for the duration of the page, renders `post.body` via `dangerouslySetInnerHTML` (mock content only — see integration notes).
- `PostCard.tsx`: exports `PostMeta` (author/category/date/read-time row, `showAuthor`/`showReadTime` opt-in props), `CardText` (shared top/bottom text block used by both featured and grid layouts), and `PostCard` (the hover-reveal grid tile).
- `BlogCover.tsx`: generic cover component — `slug` picks the deterministic mesh preset; accepts `size` (square) or explicit `width`/`height` (for the non-square hero); accepts `children` for custom overlays (grid card title/reveal) or falls back to a centered `text` label with a scrim (featured/hero).
- `cover.ts`: `hash(str)` (djb2-style) → deterministic index into `DEFAULT_MESH_PRESETS`; exports `GRAIN_DATA_URI` (inline SVG fractal-noise, desaturated).
- `blogData.ts`: mock `BlogPost[]` (`POSTS`), `sortedPosts()`, `authorFor`/`initials` (deterministic mock authors with role), `formatDate`/`shortDate` helpers. No CMS — flagged in-repo as prototype-only, production will need a real data source.
- `Nav.tsx`: `Panel` type narrowed to `'products' | 'explore' | null`; new `EXPLORE_ITEMS` config (Use cases/Blog/Help center, Blog uses SPA `<Link>`, others placeholder `href="#"`); Explore trigger renders an anchored `s.exploreMenu` dropdown instead of joining the expanding-panel behavior; added a direct Blog `<Link>` nav item; mobile drawer given matching Explore accordion + direct Templates/Blog rows.
- `Nav.module.css`: the scrolled-pill `overflow: hidden` clip is now scoped to `.navWrap.isScrolled .navBar.productsOpen` (previously applied more broadly), specifically so it doesn't clip the new floating `.exploreMenu`; added `.exploreMenu`/`.exploreMenuItem` styles.
- Added/curated `.agents`/`.claude` design-review skills (userinterface-wiki, rams, fixing-accessibility, fixing-metadata, emil-design-eng, review-animations) and a `SessionStart` hook (`scripts/install-hooks.sh` via `.claude/hooks/session-start.sh`) that re-activates `.githooks` on every cloud session — repo tooling only, no runtime/UI impact.

### Files touched
- `src/App.tsx` (modified) — registers `/blog` and `/blog/:slug` routes.
- `src/components/Nav.tsx` (modified) — Explore dropdown, Blog link, Features tab removed, mobile drawer mirror.
- `src/components/Nav.module.css` (modified) — floating explore-menu styles, scoped overflow-clip fix.
- `src/routes/blog/BlogIndex.tsx` (new) — homepage: featured post + paginated grid.
- `src/routes/blog/BlogPost.tsx` (new) — detail page: gradient hero, body, read-next.
- `src/routes/blog/PostCard.tsx` (new) — `PostMeta`, `CardText`, `PostCard`.
- `src/routes/blog/BlogCover.tsx` (new) — generated cover component (mesh + grain + overlay).
- `src/routes/blog/cover.ts` (new) — deterministic mesh-preset hash + grain SVG data URI.
- `src/routes/blog/blogData.ts` (new) — mock post data, authors, date/format helpers.
- `src/routes/blog/blog.css` (new, 706 lines) — full blog + post-body typography system.
- `.agents/skills/**`, `.claude/**`, `skills-lock.json`, `handoffs/**` (new) — agent tooling/process only, not shippable UI.

### Integration notes for Curry
- **DESIGN.md was not updated** in this push — blog page/component specs (cover generation, post-body typography, card layout) exist only in code/CSS. Treat `blog.css` and the components above as the source of truth until DESIGN.md is backfilled.
- Blog content is 100% mock (`blogData.ts`), including `body` as hand-authored trusted HTML rendered via `dangerouslySetInnerHTML` — production needs a real CMS/sanitization step before this pattern is safe with untrusted content.
- Covers depend on `src/sections/gradient-generator/` (`MeshCanvas`, `DEFAULT_PALETTE`, `DEFAULT_MESH_PRESETS`) — don't rebuild the gradient logic standalone; the blog reuses that module directly.
- Nav's Explore item "Use cases" and "Help center", and the Templates tab, are still placeholder `href="#"` links — only Blog is actually wired to a route.
- The `.githooks`/SessionStart hook and design-review skills are dev-environment tooling (handoff-note generation, agent design linting) — not part of the shipped app; skip them in the production rebuild.
