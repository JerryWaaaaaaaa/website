## 2026-07-02 00:11 — push bead126..105453c (1 commit)

### Design changes
- No visual or design-token changes. The voice-over dropdown's open/collapsed states already existed (driven by `data-open` in VoiceOverPanel.css); this push only wires up the control that toggles between them.

### Code changes
- `src/sections/ProductSuiteV5C.tsx`: added a `voiceOpen` boolean state (`useState(false)`) and passed it to `<VoiceOverPanel>` as `open={voiceOpen} onToggle={() => setVoiceOpen((o) => !o)}`. Previously the panel was rendered as `<VoiceOverPanel />` with no props.
- Root cause: `VoiceOverPanel` (`src/sections/productsuite/voiceoverpanel.tsx`) declares `open: boolean` and `onToggle: () => void` as **required** props. Rendering it with no props was a TypeScript error that failed the production build ("fixed deploy error"). This push supplies the required props to unblock the build.
- The panel is a controlled component: parent owns open/collapsed state; `onToggle` fires on the "Select Voice-over" button click (`aria-expanded={open}`), so users can open/collapse after the intro sequence. Panel body is a static exported image (`/slides-mockup/voiceover/panel.png`); only the button is a real DOM control.

### Files touched
- `src/sections/ProductSuiteV5C.tsx` (modified) — added `voiceOpen` state and passed `open`/`onToggle` props to `VoiceOverPanel`.
- `tsconfig.app.tsbuildinfo` (modified) — incremental build cache; `errors:true` flag cleared to no error flag. Build artifact only, not source.

### Integration notes for Curry
- **No new dependencies.** Pure state/prop wiring using existing React `useState`.
- `tsconfig.app.tsbuildinfo` is a generated TS incremental-build artifact — don't hand-edit or port it; it should be gitignored in production. The meaningful `errors:true → (removed)` delta just reflects the type error being fixed.
- `VoiceOverPanel` also accepts an optional `visible` prop (defaults `true`) that fades the whole control in — intended to be driven by the Slides intro sequence. It's currently left at default here; confirm with Jerry whether the intro should toggle `visible`/`open` on a timer in production, or if user-click-only is the intended final behavior.
- The dropdown panel is a pixel-exported PNG (Figma node 3321:29415), not a real list — prototype-only. Production will need a real, accessible voice-picker component; only the toggle button and its open/collapsed CSS states are reusable.
