Now I have enough grounding to write the handoff note.

## 2026-08-07 00:53 — push dc67c55..60c14f9 (32 commits)

### Design changes
- New blog homepage and detail pages, restyled iteratively to their final state: white page background (`--bg-neutral`) with a subtle brand-blue dot-grid texture, masthead capped at 560px so the h1 wraps to two lines.
- Featured post is a white card (`--color-white`, `--stroke` border, `--elevation-1`, `--rounded-3xl`) with text left / square (1:1) mesh-gradient cover flush right, only the cover's right corners rounded.
- Grid + featured covers use the Gradient Generator's `MeshCanvas` + `DEFAULT_PALETTE` (on-brand blue/lavender stops) instead of bespoke palettes, with a grain/dither texture layered on top (`.bc-grain`, SVG `feTurbulence` data URI).
- Grid cards: gradient tile is the card itself — title only at rest, description + author avatar/name/role reveal on hover/focus (always visible on touch via `@media (hover: none)`).
- Blog detail page: full markdown typography system in `.post-body` (h3/h4, mark highlight, inline code + dark-token code blocks recolored to a light scheme, blockquote with oversized decorative quote mark, callout cards, tables, figures/captions, hr) — all built from existing tokens.
- Detail hero: full-bleed mesh gradient "band" (capped with `clamp()`) carrying back-link + meta, with title/description dropped onto the white surface below on all viewports (mobile-specific overlay layout was tried and then replaced by this unified band).
- Navbar: dropped the "Features" tab; "Explore" converted from a full-width expanding bar panel into a floating anchored dropdown (Use cases / Blog / Help center); added top-level "Templates" and "Blog" links. Mirrored in the mobile drawer.
- Added a product-filter pill row to the blog index, reusing product icons/colors from the new shared `src/data/products.ts` (also refactored `ProductSuiteV5D` to consume it, no visual change there).
- Installed/pruned Claude Code design-review skills (`userinterface-wiki`, `rams`, `fixing-accessibility`, `fixing-metadata`, `emil-design-eng`, `review-animations`) — tooling only, no runtime effect.

### Code changes
- `src/routes/blog/blogData.ts` (new): mock post data, deterministic `authorFor`/`initials`/`shortDate`/`formatDate` helpers, `sortedPosts()`, optional `product` field per post. No CMS — production will need a real data source.
- `src/routes/blog/cover.ts` (new): deterministic slug→mesh-preset hash (`meshFor`) + grain texture data URI.
- `src/routes/blog/BlogCover.tsx` (new): wraps `MeshCanvas` from the gradient generator; accepts `size` (square) or explicit `width`/`height` (hero), plus `children` for custom overlay content or a default centered `text` label.
- `src/routes/blog/PostCard.tsx` (new): `PostMeta`, `CardText`, `PostCard` — shared presentational pieces for grid card, featured card, and detail-page meta row.
- `src/routes/blog/BlogIndex.tsx` (new): homepage — featured post + paginated grid (`PER_PAGE = 9`), `?product=` and `?page=` URL params, invalid product param falls back to "All".
- `src/routes/blog/BlogPost.tsx` (new): detail page by `:slug`; renders `post.body` via `dangerouslySetInnerHTML` (mock content is trusted/authored, prototype only — needs sanitization if body ever becomes user- or CMS-supplied); "Read next" row picks the next 3 posts by index, wrapping.
- `src/data/products.ts` (new): single source of truth (`PRODUCTS`, `productFor`) for product key/label/icon/color, shared by `ProductSuiteV5D.tsx` and `BlogIndex.tsx`.
- `src/components/Nav.tsx`: removed `'features'` from the `Panel` union and its whole panel/pill implementation; `Explore` now renders an anchored `s.exploreMenu` dropdown (`EXPLORE_ITEMS` config, mix of router `Link` and placeholder `href="#"`); added direct `Templates`/`Blog` nav items and matching mobile-drawer sections.
- `src/App.tsx`: added `/blog` and `/blog/:slug` routes.
- `.claude/`, `.agents/skills/`, `skills-lock.json`, `handoffs/`: agent tooling/config, not shipped to production.

### Files touched
- `src/routes/blog/BlogIndex.tsx` (new) — homepage: masthead, product filter, featured post, grid, pagination
- `src/routes/blog/BlogPost.tsx` (new) — detail page: gradient hero band, article body, read-next
- `src/routes/blog/PostCard.tsx` (new) — shared card/meta components
- `src/routes/blog/BlogCover.tsx` (new) — mesh-gradient cover component
- `src/routes/blog/cover.ts` (new) — deterministic preset/grain helpers
- `src/routes/blog/blogData.ts` (new) — mock post data + author/date helpers
- `src/routes/blog/blog.css` (new, 804 lines) — full blog styling (page shell, cards, filter, pagination, post-body typography system)
- `src/data/products.ts` (new) — shared product identity (key/label/icon/color)
- `src/sections/ProductSuiteV5D.tsx` (modified) — builds its product list from `src/data/products.ts` instead of an inline list
- `src/components/Nav.tsx` (modified) — Explore dropdown redesign, Features tab removed, Templates/Blog links added
- `src/components/Nav.module.css` (modified) — styles for the floating `exploreMenu`, scoped `overflow: hidden` clip to products-open only
- `src/App.tsx` (modified) — `/blog`, `/blog/:slug` routes
- `.claude/`, `.agents/skills/*`, `skills-lock.json`, `handoffs/*.md` (new) — agent/repo tooling, not part of the shipped UI

### Integration notes for Curry
- No new npm dependencies — blog reuses existing `MeshCanvas`/gradient-generator code, `react-router-dom`, and the site's `Nav`/`Footer`/`Chip`/General Sans setup.
- `blogData.ts` is 100% mock/static data (including `post.body` as hand-authored HTML strings injected via `dangerouslySetInnerHTML`); production needs a real CMS/content pipeline and, if body content ever comes from an untrusted or third-party source, HTML sanitization before render.
- The `Explore` dropdown's "Use cases" and "Help center" links, and the new top-level "Templates" link, are all placeholder `href="#"` — only "Blog" is wired to a real route. Confirm real destinations before launch.
- Blog product filter depends on `src/data/products.ts` staying in sync with `ProductSuiteV5D`'s product set — if products are added/removed/recolored, update that one file rather than either consumer.
- Visual end-state to build from is the final commit (60c14f9): white page bg with dot texture, white/stroke/elevation-1 featured card, square mesh covers with grain overlay, hover-reveal grid cards, and the unified gradient-band post hero on all viewports — several earlier commits in this push (light-blue page, dark featured panel, mobile-only hero split) were superseded and can be ignored.
