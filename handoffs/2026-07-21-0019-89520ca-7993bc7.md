## 2026-07-21 00:19 — push 89520ca..7993bc7 (1 commit)

### Design changes
- Introduced a dedicated brand icon color token `--color-icon-brand` / `icon-brand` = `#0D6BDE`, a slightly deeper blue than `--color-blue` (`#0C5CFF`). Per DESIGN.md, `--color-blue` stays reserved for UI accents, links, and CTA bands; `--color-icon-brand` is now the canonical fill for brand product-icon SVGs.
- Recolored all four hero/product SVG icons (Hub, Meeting/Video, Calendar, Mail) from `#0C5CFF` → `#0D6BDE` so they read consistently as a set.
- Updated the Hub nav product card and its icon tile tint from `rgba(12, 92, 255, 0.06)` → `rgba(13, 107, 222, 0.06)` to match the new icon-brand hue.

### Code changes
- No component logic, props, or state changes. Changes are limited to static SVG asset fills, one CSS-module color pair, one CSS custom property declaration, and DESIGN.md documentation.
- `src/components/Nav.module.css`: updated `.productCardHub` background and `.productCardHub .productIcon` background to the new RGBA tint.
- `src/index.css`: added `--color-icon-brand: #0d6bde;` to the color-token block.

### Files touched
- `DESIGN.md` (modified) — added `icon-brand` token to the tokens list + palette table, and an "Icons" usage note distinguishing icon-brand from `--color-blue`.
- `src/index.css` (modified) — added `--color-icon-brand: #0d6bde` custom property.
- `src/components/Nav.module.css` (modified) — Hub product card + icon tile tint bumped to `rgba(13, 107, 222, 0.06)`.
- `public/Icon/Hub.svg` (modified) — 4 paths recolored `#0C5CFF` → `#0D6BDE`.
- `public/Icon/Video.svg` (modified) — 3 paths recolored (this is the Meeting icon).
- `public/Icon/icon_calendar.svg` (modified) — single path recolored.
- `public/Icon/icon_mail.svg` (modified) — single path recolored.

### Integration notes for Curry
- The `#0D6BDE` value is **baked directly into the SVG files** as hardcoded `fill` attributes — it is NOT wired to the `--color-icon-brand` CSS variable at runtime. In production, decide whether to keep static fills or convert icons to `currentColor`/`fill: var(--color-icon-brand)` for a single source of truth; if you convert, keep the token and the SVGs in sync.
- Keep the two blues distinct: `--color-icon-brand` (`#0D6BDE`) is for brand product icons only; `--color-blue` (`#0C5CFF`) remains for accents/links/CTA. Don't collapse them.
- The Nav card tint `rgba(13, 107, 222, 0.06)` is the RGB decomposition of `#0D6BDE` at 6% — if you centralize this, derive it from the token rather than re-hardcoding.
- No dependencies to install; purely a visual token/asset update, production-ready as-is.
