Now I have enough to write the note accurately.

## 2026-08-06 04:50 — push dc67c55..466b83a (21 commits)

### Design changes
- New `/blog` and `/blog/:slug` routes on a light system: page background is white with a subtle dot-grid texture; the only tinted surface left is the featured-post panel, which uses `--bg-accent-medium` (`#E3EDFC`).
- Blog covers are square (1:1) mesh gradients generated by the existing Gradient Generator (`MeshCanvas` + `DEFAULT_PALETTE` — the four brand gradient stops), deterministically chosen per post slug, with a grain/dither SVG overlay layered on top for a "shader-like" texture.
- Grid cards (`PostCard`): the gradient tile *is* the card — title only at rest; description + author avatar reveal on hover/focus (shown by default on touch, since touch has no hover).
- Featured post: white/tinted card, text left + square cover right, cover's right corners rounded (`--rounded-3xl`), flush left edge (sharp divide, no border) against the panel.
- Blog detail hero: the post's mesh cover renders full-bleed as a wide (1120×520) background behind meta/title/description, with a linear fade to white as it scrolls into the body — mirrors the existing homepage hero-to-white handoff pattern.
- Full `.post-body` markdown typography system added (all built from existing tokens): h3/h4, em, `mark` (lavender highlight), links, inline code + light-scheme syntax-highlighted code blocks, ordered/unordered/nested lists, figures + captions, callout asides, tables with captions, hr, and a redesigned blockquote (oversized decorative opening quote mark, centered on the text, small closing mark colored to match body text — no colored left bar).
- Product filter pills atop the blog index reuse the product suite's icons/labels/brand colors; the active pill tints in that product's color, matching the product-suite tab bar's visual language.

### Code changes
- `src/routes/blog/blogData.ts`: mock post data module (`POSTS`, `sortedPosts`, `authorFor`, `initials`, `formatDate`/`shortDate`). Posts carry an optional `product?: ProductKey` field; `body` is trusted/authored HTML rendered via `dangerouslySetInnerHTML` (prototype only — no sanitization, no CMS).
- `src/routes/blog/cover.ts`: `meshFor(seed)` hashes a string (slug) to deterministically pick a `MeshPreset` from `DEFAULT_MESH_PRESETS`; exports the grain-texture data URI.
- `src/routes/blog/BlogCover.tsx`: renders `MeshCanvas` + grain overlay; accepts either `text` (centered label, used by featured/hero) or arbitrary `children` (used by grid cards for the hover-reveal content); accepts `size` (square) or explicit `width`/`height` (non-square, used only by the detail-page hero).
- `src/routes/blog/PostCard.tsx`: exports `PostMeta` (author/category/date meta row, `showAuthor`/`showReadTime` toggles), `CardText` (top-anchored eyebrow+title / bottom-anchored description+author, shared by featured panel and — previously — grid cards), and `PostCard` (the hover-reveal grid tile).
- `src/routes/blog/BlogIndex.tsx`: homepage with masthead, product-filter pill row (`?product=` URL param, invalid values fall back to "All"), featured post (page 1 + unfiltered only), 9-per-page grid (`PER_PAGE = 9`), pagination. Filtering resets to page 1 via the `blogSearch` helper.
- `src/routes/blog/BlogPost.tsx`: detail page by `:slug`; gradient hero header, `post-body` HTML render, "Read next" row (next 3 posts, wrapping, excluding self).
- `src/data/products.ts` (new): single source of truth for product `key`/`label`/`icon`/`color`, consumed by both `BlogIndex`'s filter and `ProductSuiteV5D`.
- `src/sections/ProductSuiteV5D.tsx`: refactored to build its product list from `PRODUCTS` in `src/data/products.ts` plus a local `SCREENS` map (screenshot paths only) — no visual change, just dedup of product identity.
- `src/App.tsx`: wires `/blog` → `BlogIndex`, `/blog/:slug` → `BlogPost`.
- Also in this push (non-UI): installed/pruned Claude Code design-review skills under `.agents/skills/` and `.claude/skills/` (`userinterface-wiki`, `fixing-accessibility`, `fixing-metadata`, plus `emil-design-eng`/`review-animations`/`rams` already present), and a `SessionStart` hook (`.claude/hooks/session-start.sh`) that re-activates `.githooks` on every cloud session. Neither affects the shipped app.

### Files touched
- `src/routes/blog/BlogIndex.tsx` (new) — blog homepage: masthead, product filter, featured post, grid, pagination
- `src/routes/blog/BlogPost.tsx` (new) — detail page: gradient hero, markdown body, read-next
- `src/routes/blog/PostCard.tsx` (new) — `PostMeta`, `CardText`, `PostCard` components
- `src/routes/blog/BlogCover.tsx` (new) — generated mesh-gradient cover component
- `src/routes/blog/cover.ts` (new) — deterministic mesh-preset picker + grain texture
- `src/routes/blog/blogData.ts` (new) — mock post data, authors, date/format helpers
- `src/routes/blog/blog.css` (new, 779 lines) — all blog page/card/detail/typography styles
- `src/data/products.ts` (new) — shared product identity (key/label/icon/color)
- `src/sections/ProductSuiteV5D.tsx` (modified) — consumes shared `PRODUCTS`, drops duplicated local list
- `src/App.tsx` (modified) — adds `/blog` and `/blog/:slug` routes
- `.agents/skills/**`, `.claude/skills/**`, `.claude/hooks/session-start.sh`, `.claude/settings.json`, `skills-lock.json` (new/modified) — dev-tooling only, no runtime/UI effect

### Integration notes for Curry
- No new npm dependencies — blog reuses existing `MeshCanvas`/`presets`/`mesh` from the gradient-generator section and existing `Nav`/`Footer`/`Chip`/tokens. Nothing to install.
- `blogData.ts` is 100% mock/hardcoded (no CMS, no API) — production needs a real content source; `body` is raw trusted HTML with no sanitization, so a CMS-backed version must sanitize or use a proper markdown/MDX pipeline before rendering.
- Cover images are procedurally generated canvases, not real assets — there are no actual blog post images/thumbnails anywhere; confirm with Jerry whether production wants real art direction per post or should keep the generated-gradient system.
- The product filter's `?product=` param silently falls back to "All" for unrecognized values — intentional graceful-degradation behavior, not a bug, if stale/typo'd links show up in analytics.
- `ProductSuiteV5D.tsx` was refactored only to dedupe product identity with the blog filter — verify no visual regression there since it touches a previously-shipped section.
