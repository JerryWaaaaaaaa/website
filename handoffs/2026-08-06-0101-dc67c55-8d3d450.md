## 2026-08-06 01:01 — push dc67c55..8d3d450 (13 commits)

### Design changes
- New `/blog` and `/blog/:slug` routes added to the site, styled entirely with existing global tokens from `src/index.css` (`--bg-*`, `--text-*`, `--stroke`, `--rounded-*`, `--page-max-width`, `--gutter`, `--section-block`) — no new tokens were introduced and **DESIGN.md was not updated** to document the blog components.
- Page background: white (`--bg-neutral`) with a subtle dot-grid texture (repeating radial-gradient, 20px grid) laid over it.
- Featured post is a card: `--bg-accent-medium` panel, text (eyebrow/title/description/author) on the left, a square 1:1 generated cover flush against the right edge with only its right corners rounded (`--rounded-3xl`); on hover the title tints `--text-highlight`.
- Grid cards (3-column, 2-column ≤900px, 1-column ≤560px) are the gradient cover itself — title sits directly on the gradient; description + author avatar reveal on hover/focus via a `max-height`/opacity transition (`var(--ease-out)`), and show by default on touch (`@media (hover: none)`).
- Covers are generated, deterministic-per-slug mesh gradients (reusing the Gradient Generator's `MeshCanvas` + `DEFAULT_PALETTE`) layered with an SVG film-grain texture (`mix-blend-mode: overlay`) and, on the featured/hero variants, a centered title in deep navy (`#0f1836`) over a soft white radial scrim for legibility. Cover canvas zooms 1.04x on card/featured hover.
- Detail page (`.post`) has a full authored-content type system in `blog.css`: h2/h3/h4, bold/italic, a lavender `<mark>` highlight (`--bg-highlight-pink`), inline code + dark (`#0f1836`) syntax-tokenized code blocks, ordered/unordered/nested lists with tinted markers, blockquote with `<cite>`, inline `<figure>`/`<img>`/caption, a callout `.post-note` box, a bordered data `<table>` with caption, and an `<hr>` divider. Mobile breakpoint (≤560px) shrinks headings, code blocks and table padding.
- Extra top padding (168px desktop / 128px ≤560px) added to both blog pages so titles clear the fixed nav.

### Code changes
- New route components: `BlogIndex` (masthead, featured post, 9-per-page grid, pagination via `?page=` search param) and `BlogPost` (renders by `:slug`, shows a "Read next" row of 3 upcoming posts) wired into `src/App.tsx`.
- `PostCard.tsx` exports `PostMeta`, `CardText` (shared eyebrow/title/description/author block used by both the featured panel and — via `BlogCover`'s `children` prop — the grid cards), and `PostCard`.
- `BlogCover.tsx` is a generic cover component: takes a `slug` (for deterministic mesh selection), optional centered `text`, or arbitrary `children` for custom overlays; `size` controls the `MeshCanvas` render resolution (aspect is CSS-driven via `className`).
- `cover.ts` provides `meshFor(seed)` (djb2-style hash → deterministic `MeshPreset` from `DEFAULT_MESH_PRESETS`) and a base64/URI-encoded SVG grain texture constant.
- `blogData.ts` is the mock data source: `POSTS` array (11 posts, HTML strings in `body`, rendered via `dangerouslySetInnerHTML`), plus `formatDate`, `shortDate`, `sortedPosts` (newest-first), and a deterministic mock-author system (`authorFor`, `initials`, `AUTHORS` pool of 6) keyed by post slug hash since posts have no real `author` field yet.
- Two posts ("500M documents" and "real-time collaboration") were rewritten as kitchen-sink showcases exercising every supported body element, to validate the `.post-body` CSS system end-to-end.

### Files touched
- `src/routes/blog/BlogIndex.tsx` (new) — homepage: masthead, featured post, grid, pagination
- `src/routes/blog/BlogPost.tsx` (new) — detail page by slug, read-next row
- `src/routes/blog/PostCard.tsx` (new) — `PostMeta`/`CardText`/`PostCard` shared card UI
- `src/routes/blog/BlogCover.tsx` (new) — generated mesh-gradient cover component
- `src/routes/blog/cover.ts` (new) — deterministic mesh-preset picker + grain texture URI
- `src/routes/blog/blogData.ts` (new) — mock posts, authors, date/slug helpers
- `src/routes/blog/blog.css` (new, 619 lines) — all blog page/card/cover/post-body styling
- `src/App.tsx` (modified) — adds `/blog` and `/blog/:slug` routes
- `.agents/skills/**`, `.claude/skills/**`, `.claude/commands/rams.md`, `skills-lock.json` (new) — AI coding-agent skill definitions (userinterface-wiki, fixing-accessibility, fixing-metadata, rams); no runtime/app code
- `.claude/hooks/session-start.sh`, `.claude/settings.json` (new) — activates `.githooks` pre-push handoff generator on cloud session start; repo tooling only
- `handoffs/2026-07-20-2029-eb47b72-94f247f.md` (new) — auto-generated handoff note from an earlier commit in this push

### Integration notes for Curry
- All blog content is mocked in `blogData.ts` with `body` as **trusted, hand-authored HTML strings** rendered via `dangerouslySetInnerHTML` — there is no CMS and no sanitization. Production needs a real content source (CMS or markdown pipeline) and either a sanitizer or a trusted authoring pipeline before this can take untrusted input.
- `BlogCover` depends on `src/routes/gradient-generator/` (`MeshCanvas`, `DEFAULT_PALETTE`, `DEFAULT_MESH_PRESETS`) — that module is a separate prototype tool on the site; confirm it's staying in production or port the mesh-rendering logic if it gets cut.
- Author data (`authorFor`/`AUTHORS`/`initials`) is a deterministic hash-based placeholder, not real bylines — replace with actual author records tied to the eventual CMS.
- **DESIGN.md was not updated** for any of this work (blog layout, cover generation, or `.post-body` typography system) — the only source of truth is the CSS/TSX itself, so Curry should treat `blog.css` + these components as the spec when rebuilding, and flag to Jerry if a written DESIGN.md entry is wanted before production.
- The skills/hooks commits (`70105da`, `b8c281f`, `94f247f`) only touch `.agents/`, `.claude/`, and `skills-lock.json` — dev-tooling for AI coding agents, not part of the shipped site; safe to ignore when rebuilding the UI.
