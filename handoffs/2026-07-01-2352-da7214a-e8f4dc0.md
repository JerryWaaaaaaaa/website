## 2026-07-01 23:52 — push da7214a..e8f4dc0 (1 commit)

### Design changes
- **New product-icon set adopted everywhere.** Added rounded-square `fill` + `mono` SVG pairs for Canvas, Data table, Paper, Sheets, Slides under `public/Icon/product-icons/`. Every product reference now points here, replacing the legacy `/Icon/product-*.svg` and `/Icon/mono/*.svg` paths (Docs→`canvas-fill`, classic-doc→`paper-fill`, sheet→`sheets-fill`). Note the brand hexes baked into the fills: Canvas `#0D6BDE`, Data table/Sheets `#23A52D`, Paper `#2057B1`, Slides `#FB327E`.
- **New shared interface-icon set** at `public/product-suite-assets/icons-interface/` (back/left, side-bar, chevron-down, star, lock, video-on, Comment, share, ellipsis, search, add, AI companion, edit-contained, link-angled, original-doc, polished-doc, Saved, table-ai) — real SVGs that replace the inline glyph components in the mockups.
- **New elevation token scale in `src/index.css`**: `--shadow-color: 20, 28, 40` (one cool near-black light source) plus `--elevation-1` through `--elevation-4` (resting chrome → floating widgets → overlay panels). Intended to replace bespoke per-component `box-shadow` values so co-visible surfaces read as one stack. **Add these to DESIGN.md as the canonical elevation tokens.**
- **Pricing copy (Basic plan)**: AI credits line changed to `"3 AI creations for Canvas"`; the two manual-creation bullets consolidated into `"Unlimited manual creation for Slides, Sheets, Paper, and Canvas"`.
- **Section variant lineup changed**: Hero gains variants `c` (V2) and `d` (V4). Product Suite default flips `c`→`d` (**Tab bar** is now default); order is Tab bar / Slider / Carousel / Product UI, and the old "Video" label is renamed "Carousel".
- New floating-reaction asset (`floating-reaction.png`); `datatable-ui.png` replaced with a much higher-res image (~104KB → ~1.05MB).

### Code changes
- **New `src/sections/ProductSuiteV5D.tsx` (+ `.css`)** — the "Tab bar" Product Suite variant. Reuses `ProductSuiteV5.css` wholesale and only adds horizontal tab-row nav. Auto-advancing underline driven by `--psuite-v5c-cycle`; `runId` state remounts the underline on every tab click so the countdown replays even on the active tab; staged Slides reveal (note → voice-over button → dropdown) with `prefers-reduced-motion` short-circuit. Product order: Paper, Slides, Sheets, Canvas, Data table.
- **New `src/sections/productSuite/DataTableMockup.tsx` (753 lines) + `.css` (928 lines)** — hand-built DOM mockup exporting `DataTableMockup`, `DataTablePieCard`, `DataTableAiPopover` (pie-chart dashboard card + "Fill column with AI" popover floats).
- **`CanvasMockup.tsx` refactor** — removed inline `<Ico>`/`<Tb>` glyph components and the `ReactNode` import; toolbar icons now `<img>` from `icons-interface`. `STAGE_W` 1600→1280 with a new `.cnv-doc-scale` 0.8× wrapper so body text keeps its authored size inside the narrower stage. New `AVATAR_STACK` presence array (ring color per collaborator). CSS shrank ~349→fewer lines as glyph styles were dropped.
- **`src/components/V5Tuning.tsx` — major panel reorg.** The monolithic `Layout` dial split into: `Hero` (`grouped.magnify`, `meeting.width`/`maxWidth`), `Built Different` (`stepHeight`), and a grouped `Product Suite` panel with collapsible subfolders `carousel`/`autoplay`/`slides`/`paper`/`sheets`/`canvas`/`datatable`. New CSS tokens emitted: `--hero-meeting-w`/`--hero-meeting-max-w`, `--bd5-step-height`, `--canvas-{pol,cmt,rxn}-{right,top,left,bottom,scale}`, `--dt-{pie,aipop}-{left,bottom,right,top,scale}`. Size dials now emit a unitless `width/base` **scale factor** (applied as a CSS transform) rather than a raw width. Autoplay `cycle` range changed to 8000–20000ms.
- **`HeroV2.tsx`** gains a `showTuner` prop (default `true`) so the `RibbonTunerPanel` is suppressed when rendered as a section variant (`<HeroV2 showTuner={false} />`).
- **`HeroV5.css`** meeting window is now token-driven (`width: var(--hero-meeting-w, 28%)`, `max-width: var(--hero-meeting-max-w, 520px)`).
- Icon-path swaps only (no logic) in `Nav.tsx`, `Hero.tsx`, `HeroV3.tsx`, `HeroV4.tsx`, `HeroV5.tsx`, `HeroV5B.tsx`, `ProductSuite.tsx`, `ProductTabs.tsx`, `PricingV5.tsx`.

### Files touched
- `public/Icon/product-icons/*.svg` (new) — 10 product fill/mono icons
- `public/product-suite-assets/icons-interface/*.svg` (new) — 18 shared UI icons
- `public/product-suite-assets/datatable-ui.png` (modified) — higher-res replacement (~1MB)
- `public/product-suite-assets/floating-reaction.png` (new) — Canvas reaction float asset
- `src/index.css` (modified) — added `--shadow-color` + `--elevation-1..4` tokens
- `src/sectionVariants.tsx` (modified) — added Hero V2/V4 + ProductSuiteV5D variants, reordered, new default `d`
- `src/components/V5Tuning.tsx` (modified) — tuning panels reorg + new float/scale tokens
- `src/components/Nav.tsx` (modified) — product-card icon paths
- `src/sections/ProductSuiteV5D.tsx` / `.css` (new) — Tab bar Product Suite variant
- `src/sections/productSuite/DataTableMockup.tsx` / `.css` (new) — Data table mockup + floats
- `src/sections/productSuite/CanvasMockup.tsx` / `.css` (modified) — img icons, 1280 stage + 0.8× doc scale, avatar stack
- `src/sections/productSuite/{Paper,Sheets,Slides}Mockup.tsx` / `.css`, `VoiceOverPanel.tsx` / `.css` (modified) — icon-path + float wiring
- `src/sections/{Hero,HeroV2,HeroV3,HeroV4,HeroV5,HeroV5B}.tsx`, `HeroV5.css` (modified) — icon paths, `showTuner` prop, token-driven meeting window
- `src/sections/{ProductSuite,ProductTabs}.tsx` (modified) — icon/mono-icon paths
- `src/sections/pricingV5/PricingV5.tsx` (modified) — Basic-plan copy + icon paths
- `src/sections/builtDifferentiate/BuiltDifferentiateV5A.css`, `useCaseV5/*.css`, `ProductSuiteV5*.css` (modified) — token/spacing tweaks
- `tsconfig.app.tsbuildinfo` (modified) — build artifact, ignore

### Integration notes for Curry
- **Icon migration is complete but old assets remain.** All references now use `/Icon/product-icons/*` and `/product-suite-assets/icons-interface/*`. The legacy `/Icon/product-*.svg` and `/Icon/mono/*.svg` files are now unreferenced — verify with a grep before deleting; nothing in this push removed them.
- **`V5Tuning` and all `useDialKit`/`tune`-param machinery is prototype-only** (a URL-driven design GUI). Do **not** ship it. But the CSS-custom-prop values it produces — Canvas/Data table float positions and `width/base` scale factors, hero meeting size, autoplay cycle — are the source of truth for final layout numbers; pull the resolved token values (or ask Jerry for the tuned `?tune=` URL) rather than eyeballing.
- **Mockups are hand-built DOM replicas** (Canvas, Data table, Paper, Sheets, Slides), not screenshots — driven by CSS custom props and staged JS timers with `prefers-reduced-motion` fallbacks. Confirm with Jerry whether production wants these live DOM mockups or static/exported images; they're heavy to maintain.
- **`datatable-ui.png` ballooned to ~1MB** — run it through image optimization before production.
- Adopt the new `--elevation-*` tokens repo-wide in production instead of the bespoke `box-shadow` values still present in individual mockup CSS (e.g. `HeroV5.css` still has a hardcoded `0 18px 48px rgba(2,6,31,0.32)`).
