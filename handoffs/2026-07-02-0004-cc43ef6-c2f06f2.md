## 2026-07-02 00:04 — push cc43ef6..c2f06f2 (1 commit)

### Design changes
- **Hero meeting panel** shrunk: fluid width nudged up `28% → 30%`, but the absolute ceiling `maxWidth` slashed `520px → 220px` — net effect is a much smaller meeting card since the px clamp now dominates at typical viewport sizes.
- **Built-Different section** tightened vertically: per-step block height `70px → 36px` (`--bd5-step-height`), roughly halving the gap between stepped titles.
- **Product Suite autoplay** slowed: carousel cycle `8000ms → 13000ms` (`--psuite-v5c-cycle`), affecting both the V5C slider countdown and the V5D tab-bar underline sweep.
- **Paper (Docs) float animation** starts sooner: comments + underline reveal `2560ms → 2080ms`.
- **Canvas floating panels** repositioned and scaled down:
  - polished: `right -1→-7`, `top 6→12`, `width 236→180`
  - comments: `right -7→-10.5`, `top 42→41.5`, `width 322→254`
  - reaction: `left -2→-4.5`, `bottom 4→-5` (now hangs below the card edge)
- **Data Table floating panels** pushed further outward and shrunk:
  - pie: `left -9→-20`, `bottom -11.5→-14.5`, `width 520→432`
  - aiPopover: `right -4→-17.5`, `top 26→31.5`, `width 300→276`

### Code changes
- Pure default-value edits to the `DialConfig` seed constants in `src/components/V5Tuning.tsx`. Each dial is a `[value, min, max, step]` tuple; only the first element (`value`) changed in every case — min/max/step ranges are untouched.
- Affected constants: `HERO_BASE.meeting`, `BUILTDIFF_BASE.stepHeight`, `AUTOPLAY_BASE.cycle`, `PAPER_BASE.timing.start`, `CANVAS_BASE.{polished,comments,reaction}`, `DATATABLE_BASE.{pie,aiPopover}`.
- No logic, prop, state, or signature changes — these feed the existing CSS custom properties (`--hero-meeting-w`, `--hero-meeting-max-w`, `--bd5-step-height`, `--psuite-v5c-cycle`, `--psuite-stage-height`, and the Canvas/DataTable panel position vars).

### Files touched
- `src/components/V5Tuning.tsx` (modified) — updated default values for 9 tuning dials across Hero, Built-Different, Product Suite autoplay, Paper, Canvas, and Data Table.

### Integration notes for Curry
- **These are the tuned "final" values** — the V5Tuning dial UI is a prototype-only authoring tool. In production, bake these numbers directly into the corresponding CSS custom properties / component styles and drop the dial harness entirely.
- **Watch the mismatched mirror constants:** `CANVAS_BASE_W = { polished: 236, comments: 322, reaction: 150 }` and `DATATABLE_BASE_W = { pie: 520, aipop: 300 }` were **not** updated to match the new widths (180/254 and 432/276). Confirm with Jerry whether these `_BASE_W` fallbacks are still used for scaling math — if so they're now stale and should track the new widths.
- Negative `right`/`left`/`bottom` values are intentional — floating panels deliberately overhang the card edges (reaction now bleeds off both left and bottom). Preserve overflow-visible on the card containers or they'll clip.
- No new dependencies; single-file change, no build config impact.
