## 2026-08-07 00:28 — push a33d50c..22dd7a5 (1 commit)

### Design changes
- Blog post title block (`.post-head`) top padding now mirrors the page band's top clearance token, `clamp(96px, 8vw, 132px)`, replacing the flat `36px` — headline sits lower with symmetric spacing to the band above.
- Mobile breakpoint override for `.post-hero-head` top padding increased from `28px` to `56px`, a lighter version of the same adjustment for phones.

### Code changes
- No component logic changes — pure CSS spacing adjustment in the blog post route styles.

### Files touched
- `src/routes/blog/blog.css` (modified) — `.post-head` padding-top changed to `clamp(96px, 8vw, 132px)`; `.post-hero-head` mobile padding-top changed to `56px`.

### Integration notes for Curry
- This value is hand-set in `blog.css`, not sourced from a DESIGN.md token — confirm whether `clamp(96px, 8vw, 132px)` should be promoted to a shared spacing var (it duplicates the band's top-clearance clamp used elsewhere) before hardcoding it again in production.
- No new dependencies, no component/prop changes — safe, isolated CSS tweak.
