## 2026-06-17 21:02 — push f7a2017..2f0d2ea (1 commit)

### Design changes
- All product icons and platform icons in `public/Icon/` are now standardized to a **48×48** intrinsic SVG viewBox (previously a mix of 16×16 and 32×32). This ensures consistent rendered sharpness when the icons are displayed at larger sizes via CSS `width`/`height`.
- Color tweaks in some icons to match updated brand assets:
  - `Hub.svg`: fill changed from `#2A2B2D` → `#000001`
  - `product-classic-doc.svg`: fill changed from `#3579FD` → `#2057B1`
  - `product-datatable.svg`: fill changed from `#019F5C` → `#23A52D`
  - `product-docs.svg`: fill changed from `#3579FD` → `#0D6BDE`
  - `product-sheet.svg`: fill changed from `#019F5C` → `#23A52D`
- Several Workspace-app icons (`product-classic-doc`, `product-datatable`, `product-docs`, `product-sheet`) now include an `opacity="0.25"` accent layer for subtle depth.

### Code changes
- No component logic changes. Only static SVG asset replacements — path data rescaled to fill the new 48×48 viewBox.

### Files touched
- `public/Icon/Hub.svg` (modified) — viewBox 32→48, recolored to near-black
- `public/Icon/icon_calendar.svg` (modified) — viewBox 32→48, paths rescaled
- `public/Icon/icon_mail.svg` (modified) — viewBox 32→48, paths rescaled
- `public/Icon/product-classic-doc.svg` (modified) — viewBox 16→48, redrawn paths, new brand blue
- `public/Icon/product-datatable.svg` (modified) — viewBox 16→48, redrawn paths, new brand green
- `public/Icon/product-docs.svg` (modified) — viewBox 16→48, redrawn paths, new brand blue
- `public/Icon/product-sheet.svg` (modified) — viewBox 16→48, redrawn paths, new brand green
- `public/Icon/product-slides.svg` (modified) — viewBox 32→48, paths rescaled

### Integration notes for Curry
- These are purely asset-level changes — no new dependencies or component API changes. Drop the new SVGs into the equivalent asset path in production.
- Icons are rendered via `<img src="...">` in many sections (`HeroV5`, `ProductSuiteV5`, `Nav`, etc.). In production, ensure the CSS `width`/`height` on `<img>` elements constrains display size (the 48px viewBox is the source resolution, not the display size).
- The color values above are from the raw SVG fills; cross-check with the latest Figma export if production uses a different handoff pipeline — some fills (`#000001` on Hub) look like near-black placeholders vs. the design-token `--text-primary`.
- The `opacity="0.25"` accent paths in `product-classic-doc`, `product-datatable`, `product-docs`, and `product-sheet` are baked into the SVG; no runtime styling needed.
