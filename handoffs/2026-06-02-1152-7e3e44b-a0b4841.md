## 2026-06-02 11:52 — push 7e3e44b..a0b4841 (1 commit)

### Code changes
- Removed `tools/convert-to-webp.sh` — a bash utility that batch-converted PNGs to WebP using macOS `sips`. This was a one-off tooling script, not referenced by the build pipeline.

### Files touched
- `public/hero-images/canvas.png` (modified) — re-exported/optimized, ~34 bytes smaller
- `public/hero-images/data-table.png` (modified) — re-exported/optimized, ~9 KB smaller
- `public/hero-images/sheets.png` (modified) — re-exported/optimized, ~67 bytes larger (likely metadata change)
- `public/hero-images/slides.png` (modified) — re-exported/optimized, ~569 bytes smaller
- `tools/convert-to-webp.sh` (deleted) — removed one-off image conversion utility

### Integration notes for Curry
- The hero images are still PNG; if production needs WebP, re-run conversion at build time or via a CDN transform — the helper script that did this locally is now gone.
- Image dimensions and content appear unchanged; the size deltas suggest re-compression or metadata stripping from the design tool (likely Figma export tweaks).
- These PNGs are referenced in the hero section components under `src/sections/`; no path changes, so no code updates needed.
- All changes are production-ready — just drop the updated assets into the equivalent public asset path in the production repo.
